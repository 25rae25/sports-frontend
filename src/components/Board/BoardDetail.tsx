import styled from "styled-components";

const BoardWrapper = styled.img`
	width: 50%;
	display: block;
`


const BoardDetail = () => {
	return(
		<BoardWrapper>
			
		</BoardWrapper>
	);
}

export default BoardDetail;