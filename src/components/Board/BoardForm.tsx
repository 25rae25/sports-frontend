import styled from "styled-components";

const BoardWrapper = styled.img`
	width: 50%;
	display: block;
`


const BoardForm = () => {
	return(
		<BoardWrapper>
			
		</BoardWrapper>
	);
}

export default BoardForm;